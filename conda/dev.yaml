name: astx
channels:
  - nodefaults
  - conda-forge
dependencies:
  - python
  - poetry >=2
  - nodejs # used by semantic-release
  - shellcheck
  - pip
  - pip:
      # distlib issue
      - paginate
