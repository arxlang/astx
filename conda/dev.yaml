name: astx
channels:
  - nodefaults
  - conda-forge
dependencies:
  - graphviz
  - python
  - poetry >=1.5
  - nodejs # used by semantic-release
  - shellcheck
  - pip
  - pip:
      # distlib issue
      - distlib
      - paginate==0.5.6
