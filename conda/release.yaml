name: astx
channels:
  - nodefaults
  - conda-forge
dependencies:
  - python >=3.9,<3.13
  - poetry >=2
  - nodejs >=18.17 # used by semantic-release
  - shellcheck
  - pip
  - pip:
      # distlib issue
      - paginate
