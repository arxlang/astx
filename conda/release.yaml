name: astx
channels:
  - nodefaults
  - conda-forge
dependencies:
  - graphviz
  - python >=3.8.1,<3.12
  - poetry >=1.5
  - nodejs >=18.17 # used by semantic-release
  - shellcheck
  - pip
  - pip:
      # distlib issue
      - paginate
